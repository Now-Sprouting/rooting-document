import React, { memo } from 'react'

import {IconContentWrapper} from './style'

export default memo(function Icon() {
    return (
        <IconContentWrapper>
            <h1>Icon</h1>
        </IconContentWrapper>
    );
})
