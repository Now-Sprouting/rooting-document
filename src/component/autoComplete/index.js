import React, { memo } from 'react'

import {AutoCompleteContentWrapper} from './style'

export default memo(function AutoComplete() {
    return (
        <AutoCompleteContentWrapper>
            <h1>AutoComplete</h1>
        </AutoCompleteContentWrapper>
    );
})
