import React, { memo } from 'react'

import {InputContentWrapper} from './style'

export default memo(function Input() {
    return (
        <InputContentWrapper>
            <h1>Input</h1>
        </InputContentWrapper>
    );
})
